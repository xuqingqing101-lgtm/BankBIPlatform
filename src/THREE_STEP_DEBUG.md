# ⚡ 三步诊断法

## 🎯 你的情况

错误信息不够详细，我们需要找出具体问题。

---

## 📋 请按顺序执行

### 第1步: 测试编译（最重要）

```bash
cd backend
test-compile-only.bat
```

**这一步会告诉我们：**
- ✅ 如果编译成功 → 代码没问题，是运行时的问题
- ❌ 如果编译失败 → 代码有问题，需要查看错误信息

---

### 第2步: 根据结果选择

#### 情况A: 编译成功 ✅

说明代码没问题，运行时出错。常见原因：

**1. 端口被占用（最常见）**
```bash
# 检查8080端口
netstat -ano | findstr :8080

# 如果有输出，说明端口被占用
# 记下最后一列的PID，然后结束进程：
taskkill /PID [PID号] /F
```

**2. 配置问题**
- 检查application.yml是否正确
- Redis配置是否已注释

**3. 数据库问题**
- H2数据库通常不会有问题
- 检查是否有其他程序锁定了H2文件

**解决后，运行：**
```bash
test-run.bat
```

---

#### 情况B: 编译失败 ❌

说明代码或依赖有问题。

**查看错误信息，常见情况：**

**1. "cannot find symbol"**
```
原因：Lombok没有生成方法
解决：
  mvn dependency:purge-local-repository -DmanualInclude="org.projectlombok:lombok"
  mvn clean compile
```

**2. "package xxx does not exist"**
```
原因：依赖缺失
解决：
  mvn clean compile -U
```

**3. "Unsupported class file major version"**
```
原因：JDK版本不对
解决：
  确认使用JDK 17
  java -version
```

---

### 第3步: 获取详细错误（如果需要）

如果前两步都无法解决，运行：

```bash
get-full-error.bat
```

这会生成`full-error-log.txt`，包含完整的错误堆栈。

**查看文件，重点找：**
- `Caused by:` - 根本原因
- `Exception in thread "main"` - 主线程异常
- 任何红色的ERROR信息

---

## 🔍 快速检查清单

在运行测试之前，快速检查：

- [ ] Java版本是17吗？ `java -version`
- [ ] Maven能运行吗？ `mvn -v`
- [ ] 8080端口空闲吗？ `netstat -ano | findstr :8080`
- [ ] 在backend目录吗？ `cd backend`

---

## 💡 最可能的原因（排序）

根据经验，95%的启动失败是这三个原因：

### 1. 端口被占用（50%）
**症状：** 应用编译成功但启动失败
**检查：**
```bash
netstat -ano | findstr :8080
```
**解决：**
```bash
taskkill /PID [进程ID] /F
# 或修改application.yml中的端口
```

### 2. Lombok问题（30%）
**症状：** 编译时"cannot find symbol"
**解决：**
```bash
mvn dependency:purge-local-repository -DmanualInclude="org.projectlombok:lombok"
mvn clean compile
```

### 3. 依赖下载失败（15%）
**症状：** 编译时"Could not resolve dependencies"
**解决：**
```bash
# 配置国内镜像（~/.m2/settings.xml）
# 然后
mvn clean compile -U
```

---

## 🚀 现在开始

**执行第一步：**
```bash
cd backend
test-compile-only.bat
```

**等待结果：**
- ✅ 编译成功 → 检查端口，然后运行test-run.bat
- ❌ 编译失败 → 查看错误信息，按照上面的方案解决

---

## 📞 报告问题

如果仍然无法解决，请提供：

1. **编译结果：** test-compile-only.bat的输出
2. **Java版本：** `java -version`的输出
3. **详细日志：** 运行get-full-error.bat生成的日志

---

## 文件说明

| 文件 | 用途 | 何时使用 |
|------|------|---------|
| `test-compile-only.bat` | 只测试编译 | **先运行这个** |
| `test-run.bat` | 测试启动 | 编译成功后运行 |
| `get-full-error.bat` | 获取详细日志 | 需要详细错误时 |
| `step-by-step.bat` | 逐步诊断 | 完整诊断流程 |

---

**从第一步开始吧！** 🎯

```bash
cd backend
test-compile-only.bat
```
