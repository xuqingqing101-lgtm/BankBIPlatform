# 🐚 Git Bash 快速参考

## ⚡ 5秒速查

### 启动后端
```bash
./backend-start.sh
```

### 启动前端
```bash
./frontend-start.sh
```

---

## 📋 完整流程

### 首次使用

```bash
# 1. 右键项目文件夹 → Git Bash Here

# 2. 添加执行权限（只需一次）
chmod +x backend-start.sh
chmod +x frontend-deploy.sh
chmod +x frontend-start.sh

# 3. 部署前端（只需一次，需要10分钟）
./frontend-deploy.sh

# 完成！
```

---

### 日常启动

```bash
# 窗口1：后端
右键项目文件夹 → Git Bash Here
./backend-start.sh

# 窗口2：前端（新开窗口）
右键项目文件夹 → Git Bash Here
./frontend-start.sh

# 浏览器自动打开 http://localhost:3000
```

---

## 🔧 常用命令

```bash
# 查看当前目录
pwd

# 列出文件
ls
ls -la

# 切换目录
cd frontend
cd ..

# 停止服务
Ctrl + C

# 添加执行权限
chmod +x script.sh
```

---

## 🎯 路径转换

```bash
# Windows → Git Bash

C:\Users\...     →  /c/Users/...
D:\Projects\...  →  /d/Projects/...

# 规则：
# - 盘符小写加 /
# - \ 改成 /
```

---

## ❌ 常见错误

### "Permission denied"
```bash
# 解决：添加执行权限
chmod +x backend-start.sh
```

### "command not found"
```bash
# 解决：检查是否在正确目录
pwd
ls  # 应该能看到 .sh 文件
```

### 中文乱码
```bash
# 解决：设置编码
export LANG=zh_CN.UTF-8
```

---

## 📊 脚本对比

| 脚本 | 用途 | 运行时间 | 频率 |
|------|------|----------|------|
| backend-start.sh | 启动后端 | 2-3分钟 | 每次 |
| frontend-deploy.sh | 部署前端 | 10分钟 | 一次 |
| frontend-start.sh | 启动前端 | 30秒 | 每次 |

---

## ✅ 检查环境

```bash
# Java（后端需要）
java -version
# 需要 17 或更高

# Node.js（前端需要）
node -v
# 需要 18 或 20

# npm
npm -v
# 需要 9+
```

---

## 🚀 一键启动

```bash
# 一行命令完成
chmod +x backend-start.sh && ./backend-start.sh

# 或
chmod +x frontend-start.sh && ./frontend-start.sh
```

---

## 💡 技巧

### Tab 自动补全
```bash
./back[Tab]  → ./backend-start.sh
```

### 查看历史命令
```bash
↑  # 上一条
↓  # 下一条
Ctrl + R  # 搜索
```

### 清屏
```bash
clear
# 或
Ctrl + L
```

---

## 📍 打开 Git Bash 的方式

### 方式1：右键菜单（推荐）⭐
```
右键项目文件夹 → Git Bash Here
```

### 方式2：开始菜单
```
开始菜单 → Git Bash
cd /c/你的项目路径
```

---

## 🎯 端口

| 服务 | 端口 | 地址 |
|------|------|------|
| 后端 | 8080 | http://localhost:8080 |
| 前端 | 3000 | http://localhost:3000 |

---

## 📚 详细指南

查看完整文档：
- [Git_Bash运行指南.md](Git_Bash运行指南.md)

---

**在项目根目录右键 → Git Bash Here → 运行脚本！** 🚀
