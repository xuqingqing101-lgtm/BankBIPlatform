# 🐚 Git Bash 运行指南

## 📋 概述

使用 Git Bash 运行前后端服务的完整指南。

---

## ✅ 准备工作

### 1. 确认已安装 Git Bash

**检查方法：**
- 右键任意文件夹
- 看是否有 "Git Bash Here" 选项

**如果没有：**
- 下载安装：https://git-scm.com/downloads
- 选择默认选项安装

---

### 2. 确认已安装 Java（后端需要）

**在 Git Bash 中运行：**
```bash
java -version
```

**应该看到：**
```
java version "17.0.x" 或更高
```

**如果没有：**
- 下载 JDK 17：https://adoptium.net/

---

### 3. 确认已安装 Node.js（前端需要）

**在 Git Bash 中运行：**
```bash
node -v
npm -v
```

**应该看到版本号：**
```
v18.x.x 或 v20.x.x
9.x.x
```

**如果没有：**
- 下载安装：https://nodejs.org/

---

## 🚀 使用 Git Bash 运行

### 方式1：右键菜单（推荐）⭐

#### 1. 打开项目目录
- 在文件管理器中打开项目文件夹

#### 2. 启动 Git Bash
- 在项目根目录空白处右键
- 选择 **"Git Bash Here"**

#### 3. 运行脚本
```bash
# 启动后端
./backend-start.sh

# 或启动前端
./frontend-start.sh
```

---

### 方式2：从开始菜单

#### 1. 打开 Git Bash
- 开始菜单 → Git Bash

#### 2. 切换到项目目录
```bash
cd /c/你的项目路径/bank-bi-platform
```

**提示：** Git Bash 使用 Unix 风格路径
- Windows: `C:\Users\...` 
- Git Bash: `/c/Users/...`

#### 3. 运行脚本
```bash
./backend-start.sh
```

---

## 📝 添加执行权限

**首次运行前，需要添加执行权限：**

```bash
# 给后端脚本添加执行权限
chmod +x backend-start.sh

# 给前端脚本添加执行权限
chmod +x frontend-deploy.sh
chmod +x frontend-start.sh
```

**只需要执行一次！**

---

## 🎯 完整操作流程

### 首次部署前端（一次性）

#### 1. 打开 Git Bash（在项目根目录）
```bash
右键项目文件夹 → Git Bash Here
```

#### 2. 添加执行权限
```bash
chmod +x frontend-deploy.sh
```

#### 3. 运行部署脚本
```bash
./frontend-deploy.sh
```

#### 4. 按提示操作
- 等待依赖安装
- 按 Enter 继续
- 等待服务启动

---

### 日常启动后端

#### 窗口1：启动后端

```bash
# 1. 打开 Git Bash（在项目根目录）
右键 → Git Bash Here

# 2. 运行后端启动脚本
./backend-start.sh

# 3. 等待启动完成
# 看到 "🏦 银行智能AI分析平台已启动" 即成功
```

**保持此窗口打开！**

---

### 日常启动前端

#### 窗口2：启动前端

```bash
# 1. 新开一个 Git Bash 窗口
右键 → Git Bash Here

# 2. 运行前端启动脚本
./frontend-start.sh

# 3. 浏览器自动打开
# 访问 http://localhost:3000
```

**保持此窗口打开！**

---

## 🔧 Git Bash 常用命令

### 导航命令

```bash
# 查看当前目录
pwd

# 列出文件
ls
ls -la  # 详细列表

# 切换目录
cd frontend           # 进入 frontend 目录
cd ..                 # 返回上级目录
cd ~                  # 回到用户主目录
cd /c/Projects/app    # 切换到绝对路径
```

---

### 文件操作

```bash
# 复制文件
cp source.txt dest.txt

# 移动/重命名
mv old.txt new.txt

# 删除文件
rm file.txt

# 创建目录
mkdir new-folder

# 查看文件内容
cat file.txt
less file.txt  # 分页查看
```

---

### 权限管理

```bash
# 添加执行权限
chmod +x script.sh

# 查看文件权限
ls -l
```

---

### 进程控制

```bash
# 停止当前运行的程序
Ctrl + C

# 查看运行的进程
ps aux | grep java    # 查找 Java 进程
ps aux | grep node    # 查找 Node 进程
```

---

## 📊 脚本说明

### backend-start.sh

**功能：**
- 检查 Java 和 Maven 环境
- 清理和编译项目
- 启动后端服务

**运行：**
```bash
./backend-start.sh
```

**停止：**
```bash
Ctrl + C
```

---

### frontend-deploy.sh

**功能：**
- 检查 Node.js 环境
- 创建目录结构
- 复制配置文件
- 安装依赖
- 启动开发服务器

**运行：**
```bash
./frontend-deploy.sh
```

**首次运行需要时间：**
- 下载依赖包（5-10分钟）

---

### frontend-start.sh

**功能：**
- 快速启动前端服务
- 检查依赖是否安装

**运行：**
```bash
./frontend-start.sh
```

**前提条件：**
- 已运行过 `frontend-deploy.sh`

---

## 🎨 彩色输出说明

脚本使用彩色输出来区分不同类型的信息：

- 🟢 **绿色** - 成功信息
- 🔴 **红色** - 错误信息
- 🟡 **黄色** - 警告/提示信息
- 🔵 **蓝色** - 步骤标题
- 🟣 **紫色** - 标题/横幅

---

## 🔍 常见问题

### Q1: 运行脚本时提示 "Permission denied"

**原因：** 脚本没有执行权限

**解决：**
```bash
chmod +x backend-start.sh
chmod +x frontend-deploy.sh
chmod +x frontend-start.sh
```

---

### Q2: 提示 "command not found: ./backend-start.sh"

**可能原因：**

1. **不在正确目录**
   ```bash
   # 检查当前目录
   pwd
   
   # 列出文件
   ls
   
   # 应该能看到 backend-start.sh
   ```

2. **文件不存在**
   ```bash
   # 检查文件是否存在
   ls -la | grep backend-start.sh
   ```

---

### Q3: Git Bash 路径转换

**Windows 路径 → Git Bash 路径：**

```bash
# Windows
C:\Users\YourName\Projects\app

# Git Bash
/c/Users/YourName/Projects/app

# 规则
C:\ → /c/
D:\ → /d/
\ → /
```

---

### Q4: 脚本运行中文乱码

**可能原因：** Git Bash 编码设置

**解决：**
```bash
# 临时设置
export LANG=zh_CN.UTF-8

# 或在 Git Bash 选项中设置
# Options → Text → Locale: zh_CN
#                → Character set: UTF-8
```

---

### Q5: 后端运行报错 "mvn: command not found"

**解决方案1：** 使用 Maven Wrapper
```bash
# 检查是否有 mvnw
ls mvnw

# 如果有，脚本会自动使用
```

**解决方案2：** 安装 Maven
- 下载：https://maven.apache.org/download.cgi
- 配置环境变量

---

### Q6: 前端安装依赖很慢

**解决：** 使用国内镜像

```bash
# 设置淘宝镜像
npm config set registry https://registry.npmmirror.com

# 验证
npm config get registry

# 重新安装
cd frontend
npm install
```

---

## 🎯 快速参考

### 一行命令启动

```bash
# 后端（需要在项目根目录）
chmod +x backend-start.sh && ./backend-start.sh

# 前端（首次部署）
chmod +x frontend-deploy.sh && ./frontend-deploy.sh

# 前端（日常启动）
chmod +x frontend-start.sh && ./frontend-start.sh
```

---

### 同时运行前后端

**方式1：两个 Git Bash 窗口**

```bash
# 窗口1
./backend-start.sh

# 窗口2（新开窗口）
./frontend-start.sh
```

---

**方式2：使用后台运行**

```bash
# 后端在后台运行
./backend-start.sh &

# 前端在前台运行
./frontend-start.sh
```

**注意：** 后台运行不推荐，不方便查看日志

---

## 📚 Git Bash vs CMD vs PowerShell

| 特性 | Git Bash | CMD | PowerShell |
|------|----------|-----|------------|
| Unix 命令 | ✅ | ❌ | ⚠️ 部分 |
| 彩色输出 | ✅ | ⚠️ 有限 | ✅ |
| 脚本格式 | .sh | .bat | .ps1 |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## ✅ 检查清单

### 启动后端前：
- [ ] 已安装 Java 17+
- [ ] 已安装 Maven（或有 mvnw）
- [ ] 在项目根目录或 backend 目录
- [ ] 脚本有执行权限

### 启动前端前：
- [ ] 已安装 Node.js 18+
- [ ] 已安装 npm
- [ ] 已运行过 frontend-deploy.sh（首次）
- [ ] frontend 目录存在
- [ ] 脚本有执行权限

---

## 🎯 最佳实践

### 1. 使用 Tab 键自动补全
```bash
# 输入部分文件名后按 Tab
./back[Tab]  →  ./backend-start.sh
```

### 2. 使用历史命令
```bash
# 按向上箭头查看历史命令
↑  # 上一条命令
↓  # 下一条命令

# 搜索历史
Ctrl + R  # 然后输入关键词
```

### 3. 创建别名
```bash
# 在 ~/.bashrc 中添加
alias backend='./backend-start.sh'
alias frontend='./frontend-start.sh'

# 重新加载配置
source ~/.bashrc

# 之后可以直接运行
backend
frontend
```

---

## 🚀 总结

### 最简单的启动流程：

```bash
# 1. 右键项目文件夹 → Git Bash Here

# 2. 首次运行添加权限
chmod +x *.sh

# 3. 启动后端（窗口1）
./backend-start.sh

# 4. 启动前端（窗口2，新开）
./frontend-start.sh

# 5. 访问浏览器
# http://localhost:3000
```

---

**现在你可以通过 Git Bash 优雅地运行前后端了！** 🎉

---

## 📚 相关资源

- **后端脚本：** [backend-start.sh](backend-start.sh)
- **前端部署：** [frontend-deploy.sh](frontend-deploy.sh)
- **前端启动：** [frontend-start.sh](frontend-start.sh)
- **Windows 脚本：** [智能启动.bat](智能启动.bat)

---

*最后更新：2026-02-09*
